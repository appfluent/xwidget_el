publish_to: 'none'

name: xwidget_el_example
description: Example usage
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  xwidget_el:
    path: ../
